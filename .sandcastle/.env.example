# Sandcastle local env — copy to .env-sandcastle (gitignored)
# Required for sandcastle agents to access GitHub + Anthropic API

ANTHROPIC_API_KEY=
GITHUB_TOKEN=

# Optional: limit which issue labels sandcastle picks up
# SANDCASTLE_LABEL_FILTER=ready-for-agent
